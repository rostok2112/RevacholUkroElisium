# Claude Code Project Instructions

This repository is Codex-first, but Claude Code can work here too.

Primary instructions live in `AGENTS.md`. Read that first, then use project-level subagents in `.claude/agents/` and skills in `.claude/skills/` when relevant.

Important:
- Do not commit extracted Disco Elysium content.
- Use specialized agents for architecture, localization, overlay UX, BepInEx, evals, and legal/safety review.
- Prefer concise implementation summaries and concrete file diffs.
