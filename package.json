{
  "name": "revachol-ukrainian-companion",
  "private": true,
  "version": "0.1.0",
  "description": "Context-first Ukrainian companion layer for Disco Elysium.",
  "scripts": {
    "check": "python scripts/check_all.py",
    "schemas": "python scripts/validate_schemas.py",
    "config": "python scripts/validate_config.py --example config/revachol.example.toml",
    "test": "python -m unittest discover -s tests -p \"test_*.py\""
  },
  "workspaces": [
    "packages/*"
  ]
}
