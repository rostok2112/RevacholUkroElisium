name: translation-eval

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "prompts/**"
      - "evals/**"
      - "specs/**"

jobs:
  synthetic-eval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add deterministic synthetic eval runner here."
